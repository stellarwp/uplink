/**
 * Resolvers for the stellarwp/uplink @wordpress/data store.
 *
 * Resolvers are called automatically by @wordpress/data when a selector
 * is invoked for the first time. Each resolver name must match a selector.
 *
 * Extended per phase:
 *   Phase 2 — getFeatures
 *
 * @see .plans/wp-data-store-features.md
 * @package StellarWP\Uplink
 */
export const resolvers = {};
